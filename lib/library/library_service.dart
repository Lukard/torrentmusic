/// Library service — high-level API over the Drift database for playlists,
/// favorites, history, and cache management.
class LibraryService {
  /// Get all playlists.
  Future<List<Map<String, dynamic>>> getPlaylists() async {
    // TODO: query Drift database
    throw UnimplementedError();
  }
}
